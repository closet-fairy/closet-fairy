# 옷장요정

## 작업 방식

- 사용자의 지시대로 구현에 임하기 전, 먼저 어떻게 구현할 것인지를 사전에 공유한다.
  - 코드의 Diff가 작아 Diff를 다 보여주어도 무리가 없다면, Diff를 모두 보여준다. diff 마크다운을 사용한다.
- 구현에 임하기 전 충분한 리서치를 진행하며, 요구사항이 모호한 경우 구현에 돌입하기 전 사용자에게 질문을 던진다.
- PR을 리뷰하거나 PR 코멘트에 답할 때는 `.claude/skills/review/SKILL.md`를 따른다.

## 커밋

- 커밋을 진행하라는 지시를 받을 경우, 바로 커밋하지 않고 커밋 메시지 및 커밋 메시지 설명을 그대로 사용자에게 공유하여, 이 메시지를 채택해도 좋은지를 사용자에게 묻는다.
  - 커밋 메시지 및 커밋 메시지 설명 시에는 이 저장소의 커밋 메시지 컨벤션을 준수한다.
- 커밋을 진행하는 경우 기본적으로 Co-author에 자신을 넣되, 아래의 경우에는 넣지 않는다.
  - 사용자가 명시적으로 넣지 말라고 지시한 경우
  - 커밋만 자신이 진행하고 대부분의 구현을 사용자가 진행하여, AI 주도의 구현물이라는 사실에 어긋나는 경우
- 커밋 메시지를 제안할 때 Co-author를 넣을지는 결과만 짧게 알린다. 기여도 등 판단 근거는 덧붙이지 않는다. 문구는 자유이나, 아래와 같은 형태를 따른다.
  - "Co-Authored-By는 넣으려고 합니다. 빼길 원하시면 말씀해주세요."
  - "Co-Authored-By는 넣지 않으려고 합니다. 넣길 원하시면 말씀해주세요."

### 커밋 메시지 컨벤션

AngularJS 커밋 컨벤션을 따르되, `design` 타입을 추가로 사용한다.

```
<type>: <subject>
```

| 타입 | 설명 |
| --- | --- |
| `feat` | 로직 추가, 변경, 삭제 등의 기능 작업 |
| `fix` | 의도된 동작과 다르게 구현된 버그 수정 |
| `design` | 기능에 영향 없는 CSS 또는 디자인 스타일 변경 |
| `style` | 로직 및 디자인과 무관한 단순 코드 포매팅 변경 |
| `refactor` | 동작은 동일하나 내부 로직이 변경된 리팩터링 |
| `perf` | 동작은 동일하나 성능을 개선한 변경 |
| `test` | 테스트 코드 추가, 수정, 삭제 |
| `docs` | .md 파일의 추가, 수정, 삭제에 한정한 문서 작업 |
| `chore` | 설정, 빌드, 주석 변경 등 그 외 작업 |

## 브랜치

브랜치를 새로 만들 때는 아래 형식으로 이름을 짓는다.

```
<type>/<branch-name>
```

- `<type>`: "커밋 메시지 컨벤션"의 타입 중 하나를 고른다.
- `<branch-name>`: 기능 또는 작업을 설명하는 이름을 영문 소문자와 숫자만으로, 케밥 케이스로 적는다.

예시

- `feat/add-credit-card`
- `fix/wrong-baseball-game-validation`

## 주석

- 주석은 코드만으로 전달하기 어려운 맥락이나 이유를 설명하기 위해 작성한다. 코드 자체로 의도를 충분히 전달할 수 있다면 주석을 생략한다.
  - 예를 들어 로직의 순서를 요약하기만 하는 주석은 적지 않는다. 반면 외부 사정 때문에 코드만 봐서는 이해하기 어려운 부분(예: 서버가 다른 단위로 값을 내려주는 경우)에는 적는다.
- 기본적으로 작업 시 임의로 주석을 신규로 삽입하지 않는다. 이는 주석에 대한 결정 권한을 사용자에게 두어 무분별한 노이즈 및 잘못된 문서화를 막기 위함이다.
  - 그 대신, 주석을 넣어야 한다고 판단하는 부분이 있었다면 구현을 한 차례 완료한 이후 한꺼번에 사용자에게 제안하는 형태로 진행하며, 그 주석을 넣어야 하는 근거를 사용자가 납득할 수 있도록 안내한다.

## 중요한 작업

- **중요한 작업의 경우 사용자의 명시적인 지시가 없으면 진행하지 않는다. 애매한 경우 사용자에게 확인차로 묻는 절차를 거치도록 하며, 보수적으로 임한다.**
  - commit
  - push
  - stage
  - `--hard` 파라미터가 포함되는 reset 작업
  - PR 생성
  - 리뷰 코멘트 및 PR 내용을 생성, 수정, 삭제하기
